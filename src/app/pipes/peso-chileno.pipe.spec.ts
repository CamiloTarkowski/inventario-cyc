import { PesoChilenoPipe } from './peso-chileno.pipe';

describe('PesoChilenoPipe', () => {
  it('create an instance', () => {
    const pipe = new PesoChilenoPipe();
    expect(pipe).toBeTruthy();
  });
});
